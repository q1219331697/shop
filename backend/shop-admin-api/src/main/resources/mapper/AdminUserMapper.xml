<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.admin.mapper.AdminUserMapper">

    <!-- 恢复已删除的管理员（绕过逻辑删除） -->
    <update id="restoreById">
        UPDATE t_admin_user SET deleted = 0 WHERE id = #{id} AND deleted = 1
    </update>

    <!-- 根据ID查询记录（包含已删除的，绕过逻辑删除） -->
    <select id="selectByIdIgnoreDeleted" resultType="com.shop.admin.entity.AdminUserEntity">
        SELECT * FROM t_admin_user WHERE id = #{id}
    </select>

    <!-- 分页查询管理员（包含已删除的，绕过逻辑删除） -->
    <select id="selectPageIgnoreDeleted" resultType="com.shop.admin.entity.AdminUserEntity">
        SELECT * FROM t_admin_user
        <where>
            <if test="username != null and username != ''">
                AND username LIKE CONCAT('%', #{username}, '%')
            </if>
            <if test="realName != null and realName != ''">
                AND real_name LIKE CONCAT('%', #{realName}, '%')
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
            <if test="deleted != null">
                AND deleted = #{deleted}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <!-- 根据ID更新记录（绕过逻辑删除） -->
    <update id="updateByIdIgnoreDeleted">
        UPDATE t_admin_user
        <set>
            <if test="username != null">username = #{username},</if>
            <if test="password != null">password = #{password},</if>
            <if test="realName != null">real_name = #{realName},</if>
            <if test="status != null">status = #{status},</if>
            <if test="deleted != null">deleted = #{deleted},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 根据ID逻辑删除记录（绕过逻辑删除查询） -->
    <update id="deleteByIdIgnoreDeleted">
        UPDATE t_admin_user SET deleted = 1 WHERE id = #{id} AND deleted = 0
    </update>

    <!-- 物理删除指定用户名前缀的测试用户（prefix 需以 e2e- 开头，调用方/Service 已校验） -->
    <delete id="deleteByUsernamePrefix">
        DELETE FROM t_admin_user WHERE username LIKE CONCAT(#{prefix}, '%')
    </delete>

</mapper>